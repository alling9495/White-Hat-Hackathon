from django.db import models
from django.utils import timezone

# Create your models here.
'''class Game(models.Model):
    authors = models.ForeignKey('auth.Users')
    password = models.CharField(max_length=200)

    def start'''
